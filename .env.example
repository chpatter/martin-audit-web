# Martin Audit System - Frontend Environment Configuration
# Copy this to .env.local for local development
# No secrets in here — all auth is handled by the backend

# Backend proxy URL (only needed for local dev — in production IIS handles this)
REACT_APP_API_BASE=http://localhost:3001/api
